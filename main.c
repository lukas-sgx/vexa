#include <stdlib.h>
#include <stdio.h>
#include <colors.h>

int scan(){
    char buffer[250];
    int c = 1;
    
    fgets(buffer, c, stdin);

}

int main(int argc, char const *argv[])
{
    int run = 1;
    printf("");


    while(run){
        scan();
    }

    return 0;
}
